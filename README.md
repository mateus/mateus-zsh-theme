# mateus-zsh-theme

### Install

Copy `mateus.zsh-theme` to `.oh-my-zsh/themes`

```
  $ .oh-my-zsh/themes/mateus.zsh-theme
```

Change the theme in `~/.zshrc`

```
  . . .
  ZSH_THEME="mateus"
  . . .
```

```
  $ source ~/.zshrc
```
